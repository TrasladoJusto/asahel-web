# Wrangler config — OpenNext Cloudflare (Next sobre Workers)
name = "asahel"
compatibility_date = "2025-03-25"
compatibility_flags = ["nodejs_compat"]

# OpenNext: worker generado
main = ".open-next/worker.js"

[assets]
directory = ".open-next/assets"
binding = "ASSETS"
